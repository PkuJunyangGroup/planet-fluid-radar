(async()=>{const el=document.querySelector('#source-status');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const when=s=>s?new Date(s).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai'}):'尚无成功记录';try{const r=await fetch('./catalog.json');if(!r.ok)throw Error();const d=await r.json();el.replaceChildren();for(const [name,s]of Object.entries(d.sources).filter(([k])=>!k.startsWith('journal:'))){const p=document.createElement('p');p.textContent=name+' · '+(s.status==='ok'?'正常':s.status==='partial'?'RSS 已更新，API 历史补抓待恢复':'抓取失败，保留历史记录')+' · 最近成功：'+when(s.last_rss_success||s.last_success);el.append(p)}document.querySelector('#journal-directory').innerHTML=Object.entries(d.journals.groups).map(([id,label])=>`<section class="source-group"><h3>${esc(label)}</h3><table><thead><tr><th>期刊</th><th>接入状态 · Crossref</th></tr></thead><tbody>${d.journals.journals.filter(j=>j.group===id).map(j=>{const s=d.sources['journal:'+j.id];return `<tr><td><a href="${esc(j.url)}" target="_blank" rel="noopener">${esc(j.name)} ↗</a><span class="source-state">ISSN ${esc(j.issn)}</span></td><td>${!s?'待首次抓取':s.status==='ok'?`正常 · 本次 ${s.fetched} 条 / 保留 ${s.retained} 条`:'抓取失败 · 保留历史数据'}<span class="source-state">最近成功 ${esc(when(s?.last_success))}${s?.coverage_since?' · 起始检索 '+esc(s.coverage_since):''}</span></td></tr>`}).join('')}</tbody></table></section>`).join('');document.querySelector('#resource-directory').innerHTML=d.journals.resources.map(r=>`<a class="directory-link" href="${esc(r.url)}" target="_blank" rel="noopener">${esc(r.name)} ↗</a>`).join('')}catch(e){el.textContent='来源状态暂时无法读取，请稍后刷新。'}})();
